PORT=3000
WS_PORT=3001

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=your-db-password
DB_NAME=flowmon

JWT_SECRET=change-me-to-a-long-random-secret
JWT_EXPIRES_IN=24h

# Geo-IP database path (optional, download from https://dev.maxmind.com/)
GEOIP_DB_PATH=

# Sync interval for WebSocket push (ms)
WS_PUSH_INTERVAL_MS=3000
